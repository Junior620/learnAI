services:
  - type: web
    name: learnai-app
    env: python
    region: frankfurt
    plan: free
    branch: main
    buildCommand: "pip install -r requirements-light.txt"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 600 --chdir backend app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SECRET_KEY
        generateValue: true
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: False
